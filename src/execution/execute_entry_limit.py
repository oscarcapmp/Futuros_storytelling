def execute_entry_limit(
    *,
    client,
    symbol: str,
    side: str,
    quantity: str,
    price: str,
    time_in_force: str,
    simular: bool,
    reduce_only: bool = False,
    context: str = ""
) -> dict:
    raise NotImplementedError("execute_entry_limit pendiente")
